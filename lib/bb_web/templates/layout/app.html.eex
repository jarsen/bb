<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>B,B. 💄</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <%= csrf_meta_tag() %>
  </head>
  <body class="bg-gray-100">
    <header class="flex justify-between py-2 items-center bg-gray-900">
      <div>
        <a href="/" class="text-gray-100 text-2xl m-4">Books, Bitches! 💄</a>
      </div>
      <div class="flex text-gray-100 text-lg">
        <%= if @current_user do %>
          <div>
            <%= link @current_user.name , to: Routes.user_settings_path(@conn, :edit), class: "m-4" %>
          </div>
          <div>
            <%= link "Log out", to: Routes.user_session_path(@conn, :delete), method: :delete, class: "m-4" %>
          </div>
        <% else %>
          <div>
            <%= link "Log in", to: Routes.user_session_path(@conn, :new), class: "m-4" %>
          </div>
        <% end %>
      </div>
    </header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </main>
    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
